(jbuild_version 1)

(library
 ((name std)
  (public_name reactjs_of_ocaml)
  (libraries (js_of_ocaml))
  (js_of_ocaml ((flags ("--pretty"))))
  (preprocess (pps (js_of_ocaml-ppx ppx_variants_conv)))))
