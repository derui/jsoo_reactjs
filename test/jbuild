(jbuild_version 1)

(alias
 ((name runtest)
  (deps (test_reactjscaml.bc.js))
  (action (run npm run test))
  ))

(executable
 ((name test_reactjscaml)
  (libraries (js_of_ocaml reactjscaml mocha_of_ocaml))
  (js_of_ocaml ((flags ("--pretty" "--source-map-inline"))))
  (preprocess (pps (js_of_ocaml-ppx)))))
